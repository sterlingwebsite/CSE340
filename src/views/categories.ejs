<%- include('partials/header') %>
<main>
  <h1><%= title %></h1>

  <section class="categories-list">
    <p>Please select a service project category:</p>
    <ul>
      <% categories.forEach(category => { %>
        <li><%= category.category_name %></li>
      <% }); %>
    </ul>
  </section>
</main>
<%- include('partials/footer') %>
